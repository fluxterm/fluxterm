/** 滚轮聚合窗口与浏览器单位到 RDP 单位的换算。 */
export const RDP_WHEEL_BATCH_MS = 8;
export const RDP_WHEEL_UNITS_PER_PIXEL = 120 / 100;
export const RDP_WHEEL_UNITS_PER_LINE = 120 / 3;
/** 本地诊断最多保留的采样窗口数，所有会话共用上限。 */
export const RDP_DIAGNOSTIC_WINDOW_LIMIT = 120;
